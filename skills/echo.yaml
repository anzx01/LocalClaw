name: echo
version: 1.0.0
description: Echo back a message
type: atomic
inputs:
  text:
    type: string
    description: The text to echo back
outputs:
  message:
    type: string
actions:
  - type: transform
    template: "{{text}}"
permissions:
  risk_level: low
